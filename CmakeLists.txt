cmake_minimum_required(VERSION 3.4.1)
#设置版本要求

project(demo)
#设置项目名称

SET (DEMO_SOURCE_DIR ${PROJECT_BINARY_DIR}/../src)
SET (DEMO_INCLUDE_DIR ${PROJECT_BINARY_DIR}/../include)
SET (CMAKE_RUNTIME_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR}/../bin)
SET (EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/../bin)
SET (PROJECT_LIBRARY_PATH ${PROJECT_BINARY_DIR}/../bin)
SET (LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/../bin)

INCLUDE_DIRECTORIES(${DEMO_SOURCE_DIR}  ${DEMO_INCLUDE_DIR})

ADD_LIBRARY(matrix.lib SHARED ${DEMO_SOURCE_DIR}/Matrix.cpp)
#ADD_LIBRARY(matrix SHARED ${DEMO_SOURCE_DIR}/Matrix.cpp)

#FIND_LIBRARY(MATRIX_LIBRARY matrix ${PROJECT_LIBRARY_PATH})
#搜索编译好的库
#SET(MUST_INCLUDE ${MATRIX_LIBRARY})

#SET (CMAKE_RUNTIME_OUTPUT_DIRECTORY ${PROJECT_BINARY_DIR}/../bin)
ADD_EXECUTABLE(demo.exe ${DEMO_SOURCE_DIR}/main.cpp)
#生成可执行文件
#TARGET_LINK_LIBRARIES(demo.exe ${MUST_INCLUDE})
MESSAGE("DEMO COMPLETED!\n")



